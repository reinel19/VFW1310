var win = Ti.UI.createWindow({
	title: "Layout Models",
	modal: true,
	backgroundColor: "#333",
});

var image = Ti.UI.createImageView({
	image: "KS_nav_ui.png"
});

//var modes = require("layouts");
win.add(image);
win.open();
