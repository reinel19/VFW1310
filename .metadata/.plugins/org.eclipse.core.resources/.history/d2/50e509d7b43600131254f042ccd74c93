var win = Ti.UI.createWindow({
	title: "Game Gallery",
	modal: true,
	backgroundColor: "#333",
});

var navGroup = Ti.UI.iPhone.createNavigationGroup({
	window: win
});

var modes = require("layouts");

win.add(navGroup);
win.open();
